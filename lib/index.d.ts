import { FC, ReactNode } from 'react';

interface Props$1 {
    children: ReactNode;
}
declare const Button: FC<Props$1>;

interface Props {
    children: ReactNode;
}
declare const Card: FC<Props>;

export { Button, Card };
